{
  "name": "home-assignment",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.45.2",
    "@types/node": "^20.14.11",
    "dotenv": "^16.4.5"
  },
  "scripts": {
    "run:test:chrome": "yarn playwright test --project=chromium",
    "open:test:ui": "yarn playwright test --ui --project='chromium'",
    "html:report": "yarn playwright show-report",
    "test-login": "yarn playwright test -g @login",
    "test-sorting": "yarn playwright test -g @sorting",
    "test-cart": "yarn playwright test -g @cart",
    "test-checkout": "yarn playwright test -g @checkout"
  }
}